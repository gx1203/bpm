<!--公用组件：回到指定位置
/**
* 按钮组件
* @desc 回到指定位置（锚点）
* @author yanzy
* @date 2019.08.28
* @param {String} [custom-style] 传入的样式
* @param {Number} [visibility-height] 图标显示的控制条件，当页面的滚动距离大于该数值是显示组件
* @param {Number} [back-position] 返回的锚点位置，默认为页面顶部
* @example 调用示例
    <portal-back-to-top :custom-style="myBackToTopStyle" :visibility-height="300" :back-position="50" transition-name="fade" />
*/
-->